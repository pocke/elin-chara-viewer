# Read by the host's docker compose, and by Next.js as an env file. Anything
# the app itself needs goes in .env.local, which git ignores.
#
# Without this pin, compose also loads compose.override.yaml if one appears,
# and a container that writes one chooses its own next set of mounts.
COMPOSE_FILE=compose.yaml
